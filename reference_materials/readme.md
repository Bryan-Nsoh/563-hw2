# Reference Materials

This directory preserves the original assignment artifacts provided by the instructor.

- `assignment_tex/` contains the LaTeX brief and PDF describing STAT 563 Project #2.
- `matlab_modules/` hosts the MATLAB scripts supplied with the assignment.
- `STAT 563 LAB PROJECT#2 FALL_2025 WRITE UP CANVAS.zip` is the untouched archive that
delivered the materials above.

All Python translations in `src/stat563_hw2/` are written to mirror the logic from these
resources while enabling automated pipelines and LaTeX report generation.
